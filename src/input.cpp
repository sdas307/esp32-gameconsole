#include "input.h"

/* The following is NOT currently being implemented
    as platformio.ini file's source filter manages
    the separate compilation of the two input
    files */
    
// #ifdef EMULATOR_BUILD
//     #include "input_emulator.cpp"
// #elif defined(DEVICE_BUILD)
//     #include "input_device.cpp"
// #endif